void dealerRule(){
  while (dealerPoints < 16){
    loadDealerCard(getCardNumber());
  }
  dealerPoints = 0;
}

void whoWon(){
  // If dealerpoints > playerPointsler wins && dealerPoints <= 21 dealer wins
} 
